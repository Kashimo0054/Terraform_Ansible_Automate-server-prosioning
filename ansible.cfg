[defaults]
# Point to your new dynamic inventory file
inventory = aws_ec2.yml
private_key_file = ./Pipelinekeypair.pem
host_key_checking = False
remote_user = ubuntu

[inventory]
# This is the CRITICAL part you are missing
enable_plugins = aws_ec2, host_list, script, auto, yaml, ini
